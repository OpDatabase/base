name: Release Management
on:
  pull_request:
    branches:
      - master

jobs:
  check-version-bump:
    runs-on: ubuntu-latest

    steps:
      - name: Danger JS Action
        uses: danger/danger-js@9.1.8
